import SwiftUI

struct FeaturedView: View {
    var body: some View {
        VStack {
            Text("🌟 FEATUREd Creators")
                .font(.largeTitle)
                .bold()
            Spacer()
        }
        .padding()
    }
}
