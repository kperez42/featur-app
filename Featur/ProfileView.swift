import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack {
            Text("👤 Your Profile")
                .font(.largeTitle)
                .bold()
            Spacer()
        }
        .padding()
    }
}
