import SwiftUI

struct MessagesView: View {
    var body: some View {
        VStack {
            Text("💬 Messages")
                .font(.largeTitle)
                .bold()
            Spacer()
        }
        .padding()
    }
}
