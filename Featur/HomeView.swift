import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("🏠 Home")
                .font(.largeTitle)
                .bold()
            Spacer()
        }
        .padding()
    }
}
