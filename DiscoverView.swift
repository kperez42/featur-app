import SwiftUI

struct DiscoverView: View {
    var body: some View {
        VStack {
            Text("🔍 Discover")
                .font(.largeTitle)
                .bold()
            Spacer()
        }
        .padding()
    }
}
